# Asset Management Components

تم إعادة هيكلة صفحة تفاصيل الأصل (AssetDetail.tsx) لتحسين قابلية الصيانة والتنظيم.

## 📁 البنية

```
components/
├── AssetImageSlider.tsx      # سلايدر الصور مع أزرار التنقل
├── AssetInfoCard.tsx          # البطاقة الرئيسية لمعلومات الأصل
├── AssetTabs.tsx              # نظام التنقل بين التبويبات
├── types.ts                   # تعريفات TypeScript
├── utilities.ts               # دوال مساعدة (formatCurrency)
├── index.ts                   # ملف التصدير الرئيسي
├── tabs/                      # مكونات التبويبات
│   ├── MetadataTab.tsx        # تبويب بيانات تعريف الأصل
│   ├── DepreciationTab.tsx    # تبويب الإهلاك
│   ├── MovementsTab.tsx       # تبويب سجل الحركات
│   ├── MaintenanceTab.tsx     # تبويب الصيانة
│   ├── ImprovementsTab.tsx    # تبويب التحسينات
│   ├── DisposalTab.tsx        # تبويب الاستبعاد/البيع
│   ├── AttachmentsTab.tsx     # تبويب المرفقات
│   ├── index.ts               # ملف التصدير
│   └── README.md              # توثيق التبويبات
└── modals/                    # مكونات النوافذ المنبثقة
    ├── AssetModals.tsx        # جميع النوافذ المنبثقة (7 modals)
    └── index.ts               # ملف التصدير
```

## 🎯 الفوائد

### قبل إعادة الهيكلة
- **1564 سطر** في ملف واحد
- صعوبة في الصيانة والتطوير
- كود مكرر ومعقد

### بعد إعادة الهيكلة
- **~180 سطر** في الملف الرئيسي (انخفاض بنسبة 88%)
- مكونات قابلة لإعادة الاستخدام
- سهولة في الصيانة والتطوير
- بنية واضحة ومنظمة

## 📦 المكونات الرئيسية

### 1. AssetImageSlider
سلايدر الصور مع:
- أزرار التنقل (السابق/التالي)
- نقاط المؤشر
- دعم صور متعددة

### 2. AssetInfoCard
بطاقة معلومات الأصل مع:
- 3 صفوف من المعلومات
- Toggler للحالة (مفعل/غير مفعل)
- عرض القيم والنسب
- شريط تقدم الاستهلاك

### 3. AssetTabs
نظام التنقل بين 7 تبويبات:
- بيانات تعريف الأصل
- الإهلاك
- سجل الحركات
- الصيانة
- التحسينات
- الاستبعاد/البيع
- المرفقات

### 4. AssetModals
جميع النوافذ المنبثقة (7):
- إعادة ضبط الإهلاك
- تغيير طريقة الإهلاك
- تغيير عدد الدفعات
- إنشاء طلب صيانة
- إضافة سجل صيانة
- تعديل قيمة الأصل
- الاستبعاد/البيع

## 🔧 الاستخدام

```tsx
import {
  AssetInfoCard,
  AssetTabs,
  MetadataTab,
  AssetModals,
  formatCurrency,
} from './components';

// في المكون الرئيسي
<AssetInfoCard
  assetData={assetData}
  isActive={isActive}
  onActiveChange={setIsActive}
  formatCurrency={formatCurrency}
/>

<AssetTabs 
  activeTab={activeTab} 
  onTabChange={setActiveTab} 
/>

{activeTab === 'metadata' && <MetadataTab assetData={assetData} />}

<AssetModals
  isResetDepreciationModalOpen={isResetDepreciationModalOpen}
  onCloseResetDepreciation={() => setIsResetDepreciationModalOpen(false)}
  // ... باقي الـ props
/>
```

## 📝 ملاحظات

- جميع المكونات تستخدم TypeScript مع تعريفات واضحة
- الأنماط متسقة عبر جميع المكونات
- دعم كامل للـ RTL (من اليمين لليسار)
- استخدام IBM Plex Sans Arabic للخط العربي

## 🚀 التطوير المستقبلي

يمكن تحسين هذه البنية بـ:
1. استخراج Header كـ component منفصل
2. إنشاء context للـ state management
3. إضافة unit tests لكل component
4. تحسين أداء re-renders باستخدام React.memo

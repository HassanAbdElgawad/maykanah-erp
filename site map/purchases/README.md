# إدارة المشتريات (Purchases Management)

## الهيكل الكامل

```
purchases/
├── suppliers/                      # الموردون
│   ├── list/                       # قائمة الموردون
│   └── add-edit/                   # إضافة/تعديل مورد
│
├── purchase-requests/              # طلب شراء
│   ├── list/                       # قائمة طلبات الشراء
│   └── add-edit/                   # إنشاء طلب مواد جديد
│
├── disbursement-requests/          # طلب صرف
│   ├── list/                       # قائمة طلبات الصرف
│   └── add-edit/                   # إنشاء طلب صرف
│
├── transfer-requests/              # طلب نقل
│   ├── list/                       # قائمة طلبات النقل
│   └── add-edit/                   # إنشاء طلب نقل
│
├── price-quote-requests/           # طلبات عروض الأسعار
│   ├── list/                       # قائمة طلبات عروض الأسعار
│   └── add-edit/                   # إنشاء طلب عروض الأسعار
│
├── supplier-price-quotes/          # عروض أسعار الموردين
│   ├── list/                       # قائمة عروض الأسعار
│   └── add-edit/                   # إنشاء عرض سعر مورد
│
├── purchase-orders/                # أوامر الشراء
│   ├── list/                       # قائمة أوامر الشراء
│   └── add-edit/                   # إنشاء أمر شراء
│
├── material-receipts/              # استلام المواد
│   ├── list/                       # قائمة استلام المواد
│   └── add-edit/                   # إنشاء استلام مواد
│
└── purchase-invoices/              # فاتورة المشتريات
    ├── list/                       # قائمة فواتير المشتريات
    └── add-edit/                   # إنشاء فاتورة مشتريات
```

## الوحدات

### 1. الموردون (Suppliers)
- قائمة الموردون
- إضافة مورد جديد
- تعديل بيانات مورد

### 2. طلب شراء (Purchase Requests)
- قائمة طلبات الشراء
- إنشاء طلب مواد جديد
- تصفية النقدية القديمة

### 3. طلب صرف (Disbursement Requests)
- قائمة طلبات الصرف
- إنشاء طلب صرف جديد

### 4. طلب نقل (Transfer Requests)
- قائمة طلبات النقل
- إنشاء طلب نقل جديد

### 5. طلبات عروض الأسعار (Price Quote Requests)
- قائمة طلبات عروض الأسعار
- إنشاء طلب عروض الأسعار

### 6. عروض أسعار الموردين (Supplier Price Quotes)
- قائمة عروض أسعار الموردين
- إنشاء عرض سعر مورد

### 7. أوامر الشراء (Purchase Orders)
- قائمة أوامر الشراء
- إنشاء أمر شراء

### 8. استلام المواد (Material Receipts)
- قائمة استلام المواد
- إنشاء استلام مواد

### 9. فاتورة المشتريات (Purchase Invoices)
- قائمة فواتير المشتريات
- إنشاء فاتورة مشتريات

## الحالة
- 🔲 الموردون
- 🔲 طلب شراء
- 🔲 طلب صرف
- 🔲 طلب نقل
- 🔲 طلبات عروض الأسعار
- 🔲 عروض أسعار الموردين
- 🔲 أوامر الشراء
- 🔲 استلام المواد
- 🔲 فاتورة المشتريات

# نظام البريد كرمب (Breadcrumb) - التقارير والإعدادات

## 📌 نظرة عامة
هذا المستند يوضح كيفية عمل نظام البريد كرمب في صفحات **التقارير** و**الإعدادات** بشكل تفصيلي.

---

## 🎯 نظام البريد كرمب للإعدادات (Settings)

### المستوى الأول: الصفحة الرئيسية
**URL:** `/settings?module=accounting`

**البريد كرمب:**
```
الإعدادات ← [Select Dropdown: إدارة الحسابات ▼]
```

- الـ Select يحتوي على قائمة الموديولات:
  - إدارة الحسابات ✓
  - إدارة المبيعات
  - إدارة المشتريات
  - إدارة المستودعات
  - إلخ...

### المستوى الثاني: صفحة فرعية
**URL:** `/settings/accounting/company`

**البريد كرمب:**
```
الإعدادات ← إدارة الحسابات ← الشركة
```

**السلوك:**
1. كل عنصر في المسار (ماعدا الأخير) هو **Link قابل للضغط**
2. عند الضغط على "الإعدادات" → يرجع إلى `/settings?module=accounting`
3. عند الضغط على "إدارة الحسابات" → يرجع إلى `/settings?module=accounting`
4. آخر عنصر (الشركة) يكون **bold** وغير clickable

⚠️ **مهم:** لا يوجد dropdown عند الضغط على العناصر، بل navigation مباشر

### المستوى الثالث: صفحة فرعية أعمق
**URL:** `/settings/accounting/budget/create`

**البريد كرمب:**
```
الإعدادات ← إدارة الحسابات ← الميزانية ← إنشاء جديد
```

**السلوك:**
- كل عنصر (ماعدا الأخير) قابل للضغط
- يرجع للصفحة المناسبة مع query parameter صحيح

---

## 📊 نظام البريد كرمب للتقارير (Reports)

### المستوى الأول: الصفحة الرئيسية
**URL:** `/reports?selected=accounting`

**البريد كرمب:**
```
التقارير ← [Select Dropdown: إدارة الحسابات ▼]
```

### المستوى الثاني: صفحة تقرير محدد
**URL:** `/reports/accounting/general-ledger`

**البريد كرمب:**
```
التقارير ← إدارة الحسابات ← الأستاذ العام
```

**السلوك:**
- عند الضغط على "التقارير" → يرجع إلى `/reports?selected=reports`
- عند الضغط على "إدارة الحسابات" → يرجع إلى `/reports?selected=accounting`
- لا يوجد dropdown، بل Links مباشرة

---

## 🔧 البنية التقنية

### الملفات الرئيسية:
1. **Breadcrumbs.tsx** - مكون البريد كرمب الرئيسي
2. **breadcrumbs.config.ts** - تعريف المسارات
3. **useBreadcrumbs.ts** - Hook لبناء البريد كرمب
4. **settings-cards.data.ts** - بيانات بطاقات الإعدادات

### المتغيرات المهمة:
```typescript
isSettingsPage = location.pathname === '/settings' || location.pathname.startsWith('/settings/')
isInSpecificSettingsModule = location.pathname.split('/').filter(Boolean).length > 2 && location.pathname.startsWith('/settings/')
```

### شروط العرض:
- **في الصفحة الرئيسية:** `isSettingsPage && breadcrumbs.length === 1 && !isInSpecificSettingsModule`
  - عرض Select Dropdown
  
- **في صفحة فرعية:** `isSettingsPage && breadcrumbs.length > 1`
  - عرض Links مباشرة بدون dropdown

### دالة getSelectedModuleFromUrl:
```typescript
const getSelectedModuleFromUrl = () => {
  const params = new URLSearchParams(location.search);
  // For Settings page, use 'module' parameter, for Reports use 'selected'
  const moduleParam = params.get('module');
  const selectedParam = params.get('selected');
  const paramValue = moduleParam || selectedParam;
  
  // Support all modules
  if (paramValue === 'accounting') return 'sidebar.accounting';
  if (paramValue === 'sales') return 'sidebar.sales';
  if (paramValue === 'purchases') return 'sidebar.purchases';
  if (paramValue === 'warehouses') return 'sidebar.warehouses';
  if (paramValue === 'workflow-engine' || paramValue === 'workflow') return 'sidebar.workflow';
  // ... etc
  
  return 'sidebar.accounting'; // default
};
```

---

## 🎨 التصميم

### Select Dropdown (المستوى الأول):
```css
width: 264px
height: 45px
background: #F1F5F9
border: 1px solid #CFCFCF
border-radius: 8px
```

### Clickable Link (المستوى الثاني):
```css
font-weight: normal (400)
color: #093738
hover: underline
```

### Current Page (آخر عنصر):
```css
font-weight: semibold (600)
color: #093738
no hover effect
```

---

## 📝 مثال كامل للتطبيق

### إدارة المبيعات - الإعدادات:

**الصفحة الرئيسية:**
```
URL: /settings?module=sales
Breadcrumb: الإعدادات ← [Select: إدارة المبيعات ▼]
```

**إعدادات المبيعات:**
```
URL: /settings/sales/sales-settings
Breadcrumb: الإعدادات ← إدارة المبيعات ← إعدادات المبيعات

السلوك:
- الضغط على "الإعدادات" → /settings?module=sales
- الضغط على "إدارة المبيعات" → /settings?module=sales
- "إعدادات المبيعات" → غير قابل للضغط (الصفحة الحالية)
```

**قالب الشروط والأحكام:**
```
URL: /settings/sales/terms-template
Breadcrumb: الإعدادات ← إدارة المبيعات ← قالب الشروط والأحكام

السلوك:
- الضغط على "الإعدادات" → /settings?module=sales
- الضغط على "إدارة المبيعات" → /settings?module=sales
- "قالب الشروط والأحكام" → غير قابل للضغط
```

---

## ⚠️ قواعد مهمة

### ✅ **الصح:**
1. كل عنصر في البريد كرمب (ماعدا الأخير) يكون Link مباشر
2. عند الضغط على أي عنصر، يتم التوجيه مباشرة للصفحة المناسبة
3. استخدام query parameters صحيحة:
   - للإعدادات: `?module=moduleName`
   - للتقارير: `?selected=moduleName`
4. آخر عنصر في المسار يكون bold وغير clickable

### ❌ **الخطأ:**
1. ~~عرض dropdown عند الضغط على العناصر الوسطى~~
2. ~~عدم استخدام query parameters~~
3. ~~التوجيه لمسار خاطئ~~
4. ~~جعل آخر عنصر clickable~~

---

## ✅ قائمة التحقق للتطبيق

عند إضافة موديول جديد للإعدادات:

- [ ] إضافة الـ routes في `App.tsx`
- [ ] إضافة الـ breadcrumb paths في `breadcrumbs.config.ts`
- [ ] تحديث `settings-cards.data.ts` مع الـ paths الصحيحة
- [ ] التأكد من أن الـ path يتبع النمط: `/settings/{module}/{feature}`
- [ ] إضافة الترجمات في `translations.json`
- [ ] اختبار البريد كرمب في كل المستويات
- [ ] التأكد من عدم وجود dropdown في الصفحات الفرعية
- [ ] التأكد من صحة query parameters

---

## 🔗 روابط مرجعية

- نفس المنطق يطبق على **التقارير** مع استبدال `settings` بـ `reports`
- نفس المنطق يطبق على **query parameter**: `module=` للإعدادات و `selected=` للتقارير
- **مهم:** الضغط على أي عنصر في البريد كرمب يجب أن يرجع مباشرة للصفحة المناسبة بدون dropdown

---

**تاريخ الإنشاء:** 27 يناير 2026  
**آخر تحديث:** 27 يناير 2026  
**آخر تعديل:** إزالة dropdown من الصفحات الفرعية واستبداله بـ Links مباشرة
